/**
 * Делифон design tokens are now hosted in `@deliphone/ui`.
 * This file re-exports them so old imports keep working; new code should
 * import from `@deliphone/ui/tokens` directly.
 */

export * from "@deliphone/ui/tokens";
