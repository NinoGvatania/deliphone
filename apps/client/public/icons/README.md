# PWA icons

Place `icon-192.png` (192×192) and `icon-512.png` (512×512) here before
shipping. Placeholder text file is fine for the scaffolding phase —
`vite-plugin-pwa` will still build, though browsers will warn about the
missing icons.
